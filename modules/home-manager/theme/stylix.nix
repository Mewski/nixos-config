{ ... }:

{
  # Stylix opacity
  stylix.opacity = {
    applications = 1.0;
    terminal = 0.75;
    desktop = 1.0;
    popups = 1.0;
  };

  # Enable Stylix theme for kitty
  stylix.targets.kitty.enable = true;

  # Enable Stylix theme for btop
  stylix.targets.btop.enable = true;

  # Enable Stylix theme for hyprland
  stylix.targets.hyprland.enable = true;
}
